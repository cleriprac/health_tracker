$('#new_food').remove();
$('#new_link_food').show();
$("#<%= current_user.id %>_total_gained").text('<%= current_user.total_gained %>');
$("#<%= current_user.id %>_net_total").text('<%= current_user.net_total %> calories');
$("#caloric_goal").text('<%= current_user.net_total %> C / <%= current_user.caloric_goal %> C')
$('#foods tr:last').after("<tr><td><%= @food.name %></td><td><%= @food.calories %> calories</td><td><%= @food.formatted_time %></td></tr>");
